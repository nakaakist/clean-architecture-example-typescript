export type CreateUserOutputData = {
  id: string;
};

export type ListAllUsersOutputData = {
  id: string;
  name: string;
}[];

export type FindUserByIdOutputData = {
  id: string;
  name: string;
};
